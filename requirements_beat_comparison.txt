# Optional dependencies for beat detection comparison

# Madmom - established beat tracking library
madmom>=0.16.1

# beat_this - newer beat tracking library from CPJKU
# Note: beat_this requires PyTorch 2.0+ to be installed first!
# Install PyTorch from: https://pytorch.org/

# Then install beat_this dependencies:
tqdm
einops
soxr
rotary-embedding-torch

# Finally install beat_this from GitHub:
# pip install https://github.com/CPJKU/beat_this/archive/main.zip

# Also recommended: install ffmpeg for audio format support
# conda install ffmpeg  OR  sudo apt-get install ffmpeg
